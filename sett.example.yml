# Example Sett configuration for M3.1+ (multi-agent support)
# Copy this to sett.yml and customize as needed

version: "1.0"

agents:
  example-agent:
    role: "Example Agent"
    image: "example-agent:latest"
    command: ["/app/run.sh"]
    bidding_strategy: "exclusive"  # Required: review, claim, exclusive, or ignore
    workspace:
      mode: ro  # Read-only workspace (safe for testing)
    # Optional: Custom environment variables
    # environment:
    #   - "DEBUG=true"
    # Optional: Resource limits
    # resources:
    #   limits:
    #     cpus: "1.0"
    #     memory: "512MB"

# Optional: Service overrides
# services:
#   redis:
#     image: "redis:7-alpine"
#   orchestrator:
#     image: "sett-orchestrator:latest"
